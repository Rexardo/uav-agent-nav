# Codex Prompt：多无人机探索算法 `dense_maze` 公平对比实验适配

> 使用说明：
>
> 1. 将 `<ABSOLUTE_PATH>` 替换为你的实际绝对路径。
> 2. 将 `<TARGET_WS>` 替换为待适配工作空间名称。
> 3. 将 `<TARGET_REPO_NAME>` 替换为待适配论文开源项目名称。
> 4. 每次只适配一个开源仓库。
> 5. 建议把本文件放在目标仓库或上级目录中，再交给 Codex 执行。

---

你现在需要完成一个多无人机探索算法的公平对比实验适配任务。

## 一、工作目录

### 参考工作空间

```text
<ABSOLUTE_PATH>/racer_ws
```

### 待适配的论文开源代码工作空间

```text
<ABSOLUTE_PATH>/<TARGET_WS>
```

### 待适配项目名称

```text
<TARGET_REPO_NAME>
```

参考工作空间 `racer_ws` 已经完成以下工作：

1. RACER 原始代码可以正常编译和运行；
2. 已加入自定义三维点云地图 `dense_maze.pcd`；
3. 已经设置好本次对比实验使用的无人机参数、传感器参数、通信参数和地图参数；
4. 已经加入实验数据记录接口，可以输出 CSV 或其他实验结果文件；
5. 已经配置好无人机初始位置、无人机数量、实验终止条件等信息。

你的目标是：

> 在不改变目标论文核心算法原理的前提下，让 `<TARGET_REPO_NAME>` 使用与 `racer_ws` 尽可能一致的外部实验条件，在 `dense_maze` 地图上运行，并输出与 `racer_ws` 格式一致的实验数据。

---

## 二、总体原则

请严格遵守以下原则：

1. `racer_ws` 只作为参考，不要修改 `racer_ws` 中的任何文件。
2. 只修改 `<TARGET_WS>` 中的代码。
3. 不要直接把 RACER 的任务分配、探索规划或轨迹规划算法复制到目标项目。
4. 不要为了让程序跑通而替换目标论文的核心模块。
5. 保留目标论文原本的：
   - 任务表示方式；
   - 任务分配算法；
   - frontier、viewpoint 或 exploration region 生成方式；
   - 路径搜索方法；
   - 轨迹优化器；
   - 多机协调机制。
6. 统一的是外部实验条件和实际物理约束，不要求不同算法的内部参数名称和数值完全相同。
7. 对不能直接统一的参数，要通过等效物理含义进行映射，并明确记录。
8. 禁止静默使用默认参数。所有关键参数都必须能够追溯到配置文件或代码位置。
9. 在修改前先检查当前 Git 状态，并在目标仓库创建独立分支，例如：

   ```bash
   git status
   git checkout -b benchmark_dense_maze
   ```

10. 不要删除原始配置文件和原始示例地图。新增独立的 `dense_maze` 实验配置。

---

## 三、第一阶段：分析 `racer_ws`

先不要修改任何代码。

请完整检查 `racer_ws`，定位并整理以下内容。

### 3.1 地图相关

找到：

- `dense_maze.pcd` 的完整路径；
- 地图坐标系；
- 地图原点；
- 地图尺寸和有效边界；
- 点云单位；
- 地图分辨率或体素分辨率；
- 障碍物膨胀方式；
- ESDF、occupancy map 或 voxel map 的分辨率；
- 未知空间是否允许搜索；
- 飞行高度范围；
- 地面和顶部边界；
- 地图加载的 launch、YAML、XML 或源代码接口。

确认 `dense_maze.pcd` 是否还经过了：

- 缩放；
- 平移；
- 旋转；
- 降采样；
- PCD 转 Octomap；
- PCD 转 Gazebo world；
- 障碍物膨胀。

### 3.2 无人机参数

找到本次实验实际使用的：

- 无人机数量；
- 无人机水平半径；
- 无人机垂直半径或高度；
- 下洗模型；
- 障碍物安全余量；
- 最小机间距离；
- 最大速度；
- 最大加速度；
- 最大 jerk；
- 最大角速度；
- 最大 yaw rate；
- 规划周期；
- 控制周期；
- 轨迹发布时间间隔；
- 碰撞检测时间步长。

不要只读取配置文件，还要确认这些参数是否真正被代码使用。

### 3.3 传感器参数

找到：

- 传感器类型；
- 最大感知距离；
- 水平视场角；
- 垂直视场角；
- 点云更新频率；
- 点云分辨率；
- 是否存在噪声；
- 遮挡处理方式；
- 地图更新频率。

### 3.4 通信参数

找到：

- 通信距离；
- 通信频率；
- 是否考虑延迟；
- 是否考虑丢包；
- 是否要求视距；
- 无人机之间交换的信息类型。

### 3.5 初始条件及终止条件

找到：

- 每架无人机的初始位置；
- 初始朝向；
- 是否有初始速度；
- 探索区域边界；
- 覆盖率终止阈值；
- 最大运行时间；
- 无 frontier 时的终止逻辑；
- 碰撞后是否终止；
- 卡死或长时间无进展的判断条件；
- 随机种子。

### 3.6 数据记录接口

搜索 `racer_ws` 中新增或修改过的实验数据记录代码，重点检查：

- CSV 文件表头；
- 每个指标的名称；
- 每个指标的单位；
- 数据统计起点和终点；
- 记录频率；
- 结果文件保存目录；
- 文件命名方式；
- 是否每架无人机分别记录；
- 是否在实验结束时汇总；
- 是否统计均值、最大值、最小值或累计值；
- 是否有碰撞、死锁、覆盖率、路径长度、规划时间等指标。

不要仅根据文件名推测。请追踪代码调用链，确认每个指标如何计算。

---

## 四、第二阶段：分析目标项目

检查 `<TARGET_WS>` 的代码结构、README、launch 文件、YAML 文件、地图文件和参数加载逻辑。

整理目标项目中与以下内容对应的接口：

- 地图加载；
- 地图格式；
- 地图分辨率；
- 无人机尺寸；
- 障碍物安全距离；
- 机间安全距离；
- 动力学约束；
- 传感器；
- 通信；
- 初始位置；
- 无人机数量；
- 实验终止条件；
- 数据统计；
- 仿真启动方式。

需要判断目标项目使用的是：

- ROS1 或 ROS2；
- Gazebo、MARSIM、RotorS、Unity 或其他仿真器；
- PCD、BT、PLY、world、CSV 或其他地图格式；
- occupancy map、Octomap、ESDF、TSDF 或自定义体素地图。

先输出一份参数对应表，格式如下：

| 物理含义 | `racer_ws` 参数及位置 | 目标项目参数及位置 | 是否可直接统一 | 适配方式 |
|---|---|---|---|---|
| 最大速度 | 待检查 | 待检查 | 是/否/部分 | 设为相同值或进行等效映射 |
| UAV 水平半径 | 待检查 | 待检查 | 是/否/部分 | 转换为碰撞球或碰撞椭球参数 |
| 障碍物安全余量 | 待检查 | 待检查 | 是/否/部分 | 映射到 ESDF 阈值或地图膨胀 |
| 感知距离 | 待检查 | 待检查 | 是/否/部分 | 修改传感器配置 |
| 通信范围 | 待检查 | 待检查 | 是/否/部分 | 修改通信模块参数 |

完成分析后，再开始修改。

---

## 五、地图适配要求

优先让目标项目直接加载 `racer_ws` 中的 `dense_maze.pcd`。

如果目标项目不支持 PCD，可以进行格式转换，例如：

- PCD 转 Octomap `.bt`；
- PCD 转 Gazebo world；
- PCD 转目标项目要求的体素地图；
- PCD 转其他点云格式。

但必须满足：

1. 不改变地图实际几何结构；
2. 不改变地图尺寸比例；
3. 不随意增加或删除障碍物；
4. 不改变通道宽度；
5. 不改变坐标轴方向；
6. 不改变飞行空间边界；
7. 不改变地图原点，除非目标框架必须进行坐标转换；
8. 坐标转换后要同步修改无人机初始位置；
9. 记录转换脚本和转换参数；
10. 转换后的地图应能与原 `dense_maze.pcd` 进行边界和尺度核对。

请新增地图转换脚本，不要手动修改点云。

建议放在：

```text
<TARGET_WS>/scripts/convert_dense_maze_map.py
```

转换脚本需要打印：

- 输入点云点数；
- 输出点数；
- 原始边界；
- 转换后边界；
- 分辨率；
- 平移量；
- 旋转量；
- 缩放系数。

缩放系数默认必须为：

```text
1.0
```

---

## 六、公平参数统一要求

请从 `racer_ws` 读取真实使用值，不要凭经验自行设置。

### 6.1 环境条件

需要尽量统一：

- 原始地图；
- 地图空间范围；
- 可飞行高度；
- 地图分辨率；
- 仿真时间步长；
- 无人机数量；
- 初始位置；
- 初始朝向；
- 随机种子；
- 实验最大时间；
- 覆盖率终止阈值。

### 6.2 无人机物理条件

需要尽量统一：

- 水平体积；
- 垂直体积；
- 最大速度；
- 最大加速度；
- 最大 jerk；
- 最大角速度；
- 最小障碍物距离；
- 最小机间距离。

### 6.3 感知条件

需要尽量统一：

- 感知距离；
- 水平视场角；
- 垂直视场角；
- 更新频率；
- 点云噪声设置。

### 6.4 通信条件

需要尽量统一：

- 通信范围；
- 通信更新频率；
- 延迟设置；
- 丢包设置。

### 6.5 安全距离等效映射

不要机械地令不同算法的 `inflation_radius` 参数相同。

真正需要统一的是实际物理安全距离。例如：

```text
effective_obstacle_clearance
= UAV 机体半径
+ 额外安全余量
```

如果目标算法通过以下方式表达安全距离：

- 地图膨胀；
- ESDF 阈值；
- 轨迹碰撞代价；
- 安全走廊；
- 碰撞椭球；
- 控制点距离约束；

请进行等效映射。

避免出现以下错误：

- 同时考虑 UAV 半径和完整膨胀距离，导致重复膨胀；
- 只设置地图膨胀，但轨迹优化器仍使用另一套安全距离；
- 只修改 launch 文件，但源代码中的硬编码值覆盖了配置；
- 目标方法的动力学限制没有真正传入轨迹优化器。

---

## 七、配置文件重构

尽量把本次对比实验的公共参数集中到一个独立配置文件中，例如：

```text
<TARGET_WS>/config/dense_maze_benchmark.yaml
```

建议包含：

```yaml
map:
  file: dense_maze.pcd
  resolution: <从 racer_ws 读取>
  frame_id: <从 racer_ws 读取>
  min_x: <从 racer_ws 读取>
  max_x: <从 racer_ws 读取>
  min_y: <从 racer_ws 读取>
  max_y: <从 racer_ws 读取>
  min_z: <从 racer_ws 读取>
  max_z: <从 racer_ws 读取>

uav:
  number: <从 racer_ws 读取>
  radius_xy: <从 racer_ws 读取>
  radius_z: <从 racer_ws 读取>
  obstacle_margin: <从 racer_ws 读取>
  minimum_inter_uav_distance: <从 racer_ws 读取>
  max_velocity: <从 racer_ws 读取>
  max_acceleration: <从 racer_ws 读取>
  max_jerk: <从 racer_ws 读取>
  max_yaw_rate: <从 racer_ws 读取>

sensor:
  range: <从 racer_ws 读取>
  horizontal_fov: <从 racer_ws 读取>
  vertical_fov: <从 racer_ws 读取>
  update_rate: <从 racer_ws 读取>

communication:
  range: <从 racer_ws 读取>
  update_rate: <从 racer_ws 读取>
  delay: <从 racer_ws 读取>
  packet_loss_rate: <从 racer_ws 读取>

experiment:
  random_seed: <从 racer_ws 读取>
  coverage_threshold: <从 racer_ws 读取>
  timeout: <从 racer_ws 读取>
  result_directory: <与 racer_ws 统一或结构一致>
```

不能放入公共配置的算法特有参数，保留在目标算法自己的配置文件中。

---

## 八、初始位置适配

从 `racer_ws` 中读取 `dense_maze` 实验使用的所有无人机初始位置和朝向。

将其转换为目标项目要求的格式。

需要检查：

- 坐标系是否相同；
- X、Y、Z 轴方向是否相同；
- 地图原点是否相同；
- 初始点是否位于自由空间；
- UAV 体积加安全余量后是否与障碍物相交；
- 多架无人机初始位置之间是否满足最小安全距离。

如果目标方法要求不同形式的初始条件，例如 mission JSON、launch 参数或 RViz Publish Point，请生成对应的自动化配置，不要依赖手动点击。

---

## 九、实验数据记录接口适配

目标项目应输出与 `racer_ws` 尽可能一致的结果文件。

首先读取 `racer_ws` 中已有接口的真实 CSV 表头和计算逻辑，然后在目标项目中实现等价统计。

要求：

1. 指标名称一致；
2. 单位一致；
3. 统计时间范围一致；
4. 实验开始时刻定义一致；
5. 实验结束时刻定义一致；
6. 路径长度积分方式一致；
7. 覆盖率计算方式一致；
8. 碰撞判断标准一致；
9. 规划时间统计范围一致；
10. 文件结构和列顺序尽量一致。

不要简单复制 RACER 内部变量，因为目标项目可能没有相同的数据结构。应根据目标项目实际状态计算等价指标。

如果 `racer_ws` 中包含以下指标，请全部适配：

- `exploration_time`；
- `coverage_ratio`；
- `total_path_length`；
- `average_velocity`；
- `minimum_obstacle_distance`；
- `minimum_inter_uav_distance`；
- `obstacle_collision_count`；
- `inter_uav_collision_count`；
- `deadlock_count`；
- `replanning_count`；
- `planning_success_count`；
- `planning_failure_count`；
- `average_planning_time`；
- `maximum_planning_time`；
- `velocity_violation_count`；
- `acceleration_violation_count`；
- `jerk_violation_count`；
- `communication_volume`。

如果某项指标在目标项目中无法可靠获取，不要伪造数据。请标记为：

```text
UNAVAILABLE
```

并说明缺失原因和需要增加的接口。

建议新增独立模块，例如：

```text
<TARGET_WS>/src/benchmark_logger.cpp
<TARGET_WS>/include/benchmark_logger.h
```

也可以使用目标项目已有的数据记录框架。

结果文件建议放在：

```text
<TARGET_WS>/results/dense_maze/
```

文件名至少包含：

- 方法名称；
- 无人机数量；
- 随机种子；
- 运行时间戳。

---

## 十、碰撞和安全指标要求

为了避免不同算法使用各自内部碰撞定义导致结果不公平，请尽量实现独立的外部碰撞评估逻辑。

### 10.1 障碍物碰撞判断

障碍物碰撞判断应基于：

- `dense_maze` 真值地图；
- UAV 实际体积；
- 统一的安全余量。

### 10.2 机间碰撞判断

机间碰撞判断应基于：

- 各 UAV 同一时刻的真实位置；
- 统一的水平和垂直安全距离；
- 统一的采样频率。

### 10.3 分别记录的安全指标

同时分别记录：

1. 实际几何碰撞；
2. 安全距离违反；
3. 最小距离。

不要只读取目标算法内部的 `collision flag`。

---

## 十一、编译和运行

完成适配后：

1. 编译目标项目；
2. 解决编译错误；
3. 不要通过注释核心模块来规避错误；
4. 创建一个独立启动文件，例如：

   ```text
   <TARGET_WS>/launch/dense_maze_benchmark.launch
   ```

5. 确保一次命令可以启动完整实验；
6. 在启动时打印本次实验关键参数；
7. 确认加载的是 `dense_maze`，而不是原项目默认地图；
8. 确认无人机数量、初始位置和动力学参数正确；
9. 确认结果文件被正常生成；
10. 至少完成一次短时间 smoke test。

如果完整仿真运行时间较长，先执行 20～30 秒的短测试，验证：

- 地图正确显示；
- UAV 正确生成；
- UAV 开始探索；
- 没有立即碰撞；
- 数据记录文件正常写入；
- 停止程序时结果可以正常保存。

---

## 十二、禁止事项

禁止执行以下操作：

- 修改 `racer_ws`；
- 改变 `dense_maze` 几何形状；
- 为目标算法单独扩大通道；
- 为目标算法降低 UAV 尺寸；
- 为目标算法提高传感范围；
- 为目标算法提高最大速度；
- 删除目标算法中的失败检测；
- 隐藏碰撞或规划失败；
- 修改目标算法终止条件而不记录；
- 将目标算法核心规划器替换为 RACER；
- 将无法获取的数据填成 0；
- 仅修改 RViz 显示而没有修改实际仿真参数；
- 仅修改 YAML，但未确认参数是否传入核心模块。

---

## 十三、最终交付内容

完成后请输出以下内容。

### 13.1 修改文件清单

列出：

- 新增文件；
- 修改文件；
- 每个文件的作用。

### 13.2 参数对应表

给出 `racer_ws` 与目标项目之间的参数映射。

### 13.3 无法完全统一的项目

明确说明：

- 哪些参数无法直接统一；
- 为什么无法统一；
- 使用了什么等效映射；
- 是否会影响公平性。

### 13.4 地图适配说明

包括：

- 原地图路径；
- 转换后地图路径；
- 地图格式；
- 地图边界；
- 地图分辨率；
- 坐标转换；
- 是否存在缩放。

### 13.5 数据接口说明

列出：

- 输出文件路径；
- CSV 表头；
- 每项指标的计算方式；
- 统计起止时刻；
- 不可用指标。

### 13.6 编译命令

给出从干净终端开始的完整编译命令。

### 13.7 启动命令

给出完整实验启动命令。

### 13.8 验证结果

说明：

- 是否编译成功；
- 是否成功加载 `dense_maze`；
- 是否正确生成全部 UAV；
- 是否开始探索；
- 是否生成实验数据文件；
- smoke test 运行了多长时间；
- 当前还存在什么问题。

### 13.9 Git diff 摘要

概括本次改动。

不要提交或 push，等待我检查。

---

## 十四、执行顺序

请按以下顺序工作：

1. 检查 `racer_ws`；
2. 检查目标项目；
3. 输出参数对应表和适配方案；
4. 再修改代码；
5. 编译；
6. smoke test；
7. 检查数据输出；
8. 输出最终报告。

遇到无法判断的参数时，先搜索：

- README；
- 论文中给出的参数；
- launch 文件；
- YAML 文件；
- C++ 头文件；
- ROS 参数读取代码；
- 地图加载代码；
- 仿真器模型参数。

不要未经说明自行猜测。
